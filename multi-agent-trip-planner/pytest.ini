[pytest]
# Auto mode for pytest-asyncio: async tests are detected without manually
# decorating each one with the loop. (Explicit @pytest.mark.asyncio still works.)
asyncio_mode = auto
testpaths = tests
