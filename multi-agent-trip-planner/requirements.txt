# ─────────────────────────────────────────────────────────────────────────────
# multi-agent-trip-planner — dependencies
#
# The use case relies on a few families of Strands pieces:
#   1. The A2A server (specialists) + the A2A client tools  ->  [a2a] extra
#   2. The local Gemma model via Ollama (specialists)        ->  [ollama] extra
#   3. The Gemini orchestrator via LiteLLM                   ->  [litellm] extra
#
# All extras are installed on the same strands-agents package.
# strands-agents-tools provides A2AClientToolProvider, the dynamic-discovery
# client the orchestrator uses to find and call the specialists at runtime.
# ─────────────────────────────────────────────────────────────────────────────

strands-agents[a2a,ollama,litellm]>=1.0.0
strands-agents-tools>=0.1.0

# Load environment variables from .env
python-dotenv>=1.0.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ── Evaluation (Strands Evals SDK, used by the evals/ scripts) ────────────────
strands-agents-evals>=1.2.0
